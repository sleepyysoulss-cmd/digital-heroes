project_id = "digital-heroes"

[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 17

[studio]
enabled = true
port = 54323

[auth]
enabled = true
site_url = "http://127.0.0.1:5173"
additional_redirect_urls = ["https://127.0.0.1:5173"]
jwt_expiry = 3600
enable_signup = true
minimum_password_length = 6

[auth.email]
enable_signup = true
enable_confirmations = false

[storage]
enabled = true
file_size_limit = "50MiB"

# Stripe calls this endpoint directly and cannot send a Supabase user JWT,
# so this is the one function deployed without JWT verification:
#   supabase functions deploy stripe-webhook --no-verify-jwt
[functions.stripe-webhook]
verify_jwt = false
